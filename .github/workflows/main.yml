on: [push]

jobs:
  weather_job:
    runs-on: ubuntu-latest
    name: A job to print weather
    steps:
      - name: Weather action step
        id: weather
        uses: vadimdez/weather-action@1
        with:
          lat: "52.521737"
          long: "13.413117"
      - name: Get the weather
        run: echo "The weather is ${{ steps.weather.outputs.weather.name }}"
